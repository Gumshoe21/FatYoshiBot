import User from '../models/User'
/*
export const incrementUserValue = (
  username: string,
  value: number,
  amount: number
) => {
  const user = User.findOneAndUpdate(
    { username },
    {
      $inc: { [`values.fatYoshiWeightContributed`]: Math.floor(1) }
    },
    {
      upsert: true,
      new: true
    }
  );
  return user;
};
*/
